const side = {
  styleClasses : "shadow"
}

const leftSide = {
  styleClass : ".left",
  selectedStyleClasses : "shadow red"
}

const rightSide = {
  styleClass : ".right",
  selectedStyleClasses : "shadow blue"
}

module.exports = {
  side : side,
  leftSide : leftSide,
  rightSide : rightSide
}